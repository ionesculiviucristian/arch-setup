<%- include('partials/header.ejs'); -%>

## Base workstation

### Official Arch packages (Base)

<% packages["base"].official.sort(sortPackages).forEach(function(package){ -%>
- <%- `[${package.label}](${package.url}): ${package.description}` -%>
<% if (package.source_url) { %>
  - <%- `[Source](${package.source_url})` -%>
<% } -%>

<% }); -%>

### External packages (Base)

<% packages["base"].external.sort(sortPackages).forEach(function(package){ -%>
- <%- `[${package.label}](${package.url}): ${package.description}` -%>
<% if (package.source_url) { %>
  - <%- `[Source](${package.source_url})` -%>
<% } -%>

<% }); -%>

### AUR packages (Base)

<% packages["base"].aur.sort(sortPackages).forEach(function(package){ -%>
- <%- `[${package.label}](${package.url}): ${package.description}` -%>
<% if (package.source_url) { %>
  - <%- `[Source](${package.source_url})` -%>
<% } -%>

<% }); -%>

## B450 I AORUS PRO WIFI workstation

### Official Arch packages (B450 I AORUS PRO WIFI workstation)

<% packages["b450-i-aorus-pro-wifi"].official.sort(sortPackages).forEach(function(package){ -%>
- <%- `[${package.label}](${package.url}): ${package.description}` -%>
<% if (package.source_url) { %>
  - <%- `[Source](${package.source_url})` -%>
<% } -%>

<% }); -%>

### Drivers (B450 I AORUS PRO WIFI workstation)

<% packages["b450-i-aorus-pro-wifi"].drivers.sort(sortPackages).forEach(function(package){ -%>
- <%- `[${package.label}](${package.url}): ${package.description}` -%>
<% if (package.source_url) { %>
  - <%- `[Source](${package.source_url})` -%>
<% } -%>

<% }); -%>

## Browser plugins

### Brave plugins

- [Linkclump](https://chromewebstore.google.com/detail/linkclump/lfpjkncokllnfokkgpkobnkbkmelfefj): Lets you open, cp or bookmark multiple links at the same time

<% browserPlugins.brave.categories.forEach(function(category){ -%>
### <%- category.label %> (Brave)

<% category.plugins.sort(sortBrowserPlugins).forEach(function(plugin){ -%>
- [<%- plugin.name %>](<%- browserPlugins.brave.url %>/<%- plugin.id %>): <%- plugin.description %>
<% plugin.extra.sort(sortBrowserPlugins).forEach(function(extra){ -%>
  - [<%- extra.name %>](<%- extra.url || `${browserPlugins.brave.url}/${extra.id}` %>): <%- extra.description %>
<% }); -%>
<% }); %>
<% }); -%>
### Firefox plugins

- [Linkclump for Firefox](https://addons.mozilla.org/en-US/firefox/addon/linkclump-for-firefox): Lets you open, cp or bookmark multiple links at the same time

<% browserPlugins.firefox.categories.forEach(function(category){ -%>
### <%- category.label %> (Firefox)

<% category.plugins.sort(sortBrowserPlugins).forEach(function(plugin){ -%>
- [<%- plugin.name %>](<%- browserPlugins.firefox.url %>/<%- plugin.id %>): <%- plugin.description %>
<% plugin.extra.sort(sortBrowserPlugins).forEach(function(extra){ -%>
  - [<%- extra.name %>](<%- extra.url || `${browserPlugins.firefox.url}/${extra.id}` %>): <%- extra.description %>
<% }); -%>
<% }); %>
<% }); -%>
## Available aliases

For a list off all available aliases, [read more here](./ALIASES.md) or run `aliases` after installation

<%- include('partials/footer.ejs'); -%>
